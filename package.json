{
  "name": "grok-plugin-cc",
  "version": "1.2.2",
  "description": "Claude Code plugin to call xAI Grok for ask, review, image/video generation, parallel fan-out, and long-form rescue. Tests use Node's built-in test runner — no external dev dependencies.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/LovelaceLoom/grok-plugin-cc",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LovelaceLoom/grok-plugin-cc.git"
  }
}
